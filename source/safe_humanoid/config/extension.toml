# Isaac Sim extension metadata placeholder.
#
# The runnable external project will declare its extension package, dependencies, version,
# and Python module entry point here after the simulator stack has been pinned.

