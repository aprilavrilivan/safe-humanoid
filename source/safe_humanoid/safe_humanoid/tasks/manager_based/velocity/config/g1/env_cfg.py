"""Unitree G1 velocity-task configuration placeholder.

The project environment will inherit the official flat-terrain G1 task, explicitly audit
and disable upstream safety-related reward penalties, and then define the clean task reward,
scenario hooks, telemetry, and termination behavior owned by this repository.
"""

