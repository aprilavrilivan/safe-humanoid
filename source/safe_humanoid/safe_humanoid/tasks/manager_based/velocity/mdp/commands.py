"""Velocity-command generators and deterministic command schedules.

Randomized training commands and reproducible evaluation scripts will implement a common
interface so every safety method sees the same task demand during comparison.
"""

